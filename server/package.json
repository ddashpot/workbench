{
  "name": "claude-code-ui",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal web UI that wraps the Claude Code CLI: chat + human-in-the-loop approval gate.",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
